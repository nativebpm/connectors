# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: WorkflowService API
    version: 0.0.1
paths:
    /nativebpm.v1.WorkflowService/CompleteTask:
        post:
            tags:
                - WorkflowService
            operationId: WorkflowService_CompleteTask
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CompleteTaskRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CompleteTaskResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /nativebpm.v1.WorkflowService/StartProcess:
        post:
            tags:
                - WorkflowService
            operationId: WorkflowService_StartProcess
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StartProcessRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StartProcessResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /nativebpm.v1.WorkflowService/StreamWorkflowEvents:
        get:
            tags:
                - WorkflowService
            operationId: WorkflowService_StreamWorkflowEvents
            parameters:
                - name: instance_id
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WorkflowEvent'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CompleteTaskRequest:
            type: object
            properties:
                instance_id:
                    type: string
                task_id:
                    type: string
                variables_json:
                    type: string
                    format: bytes
        CompleteTaskResponse:
            type: object
            properties:
                instance_id:
                    type: string
                current_state:
                    type: string
                completed:
                    type: boolean
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        StartProcessRequest:
            type: object
            properties:
                definition_key:
                    type: string
                business_key:
                    type: string
                variables_json:
                    type: string
                    format: bytes
        StartProcessResponse:
            type: object
            properties:
                instance_id:
                    type: string
                definition_hash:
                    type: string
                current_state:
                    type: string
                completed:
                    type: boolean
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        WorkflowEvent:
            type: object
            properties:
                event_id:
                    type: string
                instance_id:
                    type: string
                node_id:
                    type: string
                node_name:
                    type: string
                node_type:
                    type: string
                action:
                    type: string
                variables_json:
                    type: string
                    format: bytes
                timestamp_ns:
                    type: string
tags:
    - name: WorkflowService
